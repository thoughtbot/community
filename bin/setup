#!/usr/bin/env sh

# Exit if any subcommand fails
set -e

cp .sample.env .env

# Set up phoenix
mix local.hex --force
mix deps.get
mix compile

# Set up database
mix do ecto.create, ecto.migrate, development_seeds

npm install --progress=false
